# 🚀 Quick Start — Sprint 1, День 3

> **Сегодня:** Среда, 10.12.2024  
> **Статус:** День 3 из 12  
> **Цель дня:** US-2.1 — Настройка монорепозитория

---

## 📋 Быстрая навигация

| Документ | Для чего |
|----------|----------|
| [SPRINT_PLANNING.md](./SPRINT_PLANNING.md) | Обзор обоих спринтов |
| [sprint-1.md](./sprint-1.md) | Детальный план Sprint 1 (текущий) |
| [DAILY_STANDUPS.md](./DAILY_STANDUPS.md) | Ежедневные отчёты |
| [WORKING_MODEL.md](./WORKING_MODEL.md) | Как мы работаем |
| [LEARNING_PLAN.md](../LEARNING_PLAN.md) | Что и когда учить |

---

## ✅ Что уже сделано

- ✅ День 1 (08.12): Документация и планирование
- ✅ День 2 (09.12): Текущий калькулятор (базовая версия)
- ✅ День 3 (10.12): План работы, спринтовая структура

---

## 🎯 Сегодняшний план (День 3)

### Задача: US-2.1 — Настройка монорепозитория
**Story Points:** 2 SP  
**Время:** 2-3 часа

**Что нужно сделать:**
1. ✅ Создать структуру папок
2. ⬜ Установить pnpm (если нет)
3. ⬜ Настроить workspaces
4. ⬜ Перенести текущие файлы в `apps/web`
5. ⬜ Создать базовую структуру для `apps/api`
6. ⬜ Проверить, что всё работает

---

## 🛠️ Пошаговая инструкция

### Шаг 1: Установить pnpm (если не установлен)

**Windows:**
```powershell
# Через npm
npm install -g pnpm

# Проверить
pnpm -v
```

**Mac/Linux:**
```bash
curl -fsSL https://get.pnpm.io/install.sh | sh
pnpm -v
```

---

### Шаг 2: Создать структуру папок

**В корне проекта выполни:**

```bash
# Создать папки
mkdir -p apps/web apps/api packages/shared

# Посмотреть структуру (в PowerShell)
tree /F
```

**Должно получиться:**
```
3D-Quotation-Ecosystem/
├── apps/
│   ├── web/          # Будущий фронтенд
│   └── api/          # Будущий бэкенд
├── packages/
│   └── shared/       # Общий код
├── docs/
├── app.js
├── index.html
├── styles.css
└── README.md
```

---

### Шаг 3: Создать корневой package.json

**Создай файл `package.json` в корне:**

```json
{
  "name": "3d-quotation-ecosystem",
  "version": "0.1.0",
  "private": true,
  "description": "Экосистема инструментов для оценки 3D-печати",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build"
  },
  "keywords": ["3d-printing", "quotation", "calculator"],
  "author": "Your Name",
  "license": "MIT"
}
```

---

### Шаг 4: Создать pnpm-workspace.yaml

**Создай файл `pnpm-workspace.yaml` в корне:**

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

---

### Шаг 5: Перенести текущие файлы в apps/web

```bash
# Переместить файлы
move index.html apps/web/
move app.js apps/web/
move styles.css apps/web/

# Создать package.json для web
cd apps/web
```

**Создай `apps/web/package.json`:**

```json
{
  "name": "@3dq/web",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend для 3D Quotation",
  "scripts": {
    "dev": "python -m http.server 3000",
    "start": "python -m http.server 3000"
  }
}
```

---

### Шаг 6: Создать структуру для API

```bash
cd ../api
```

**Создай `apps/api/package.json`:**

```json
{
  "name": "@3dq/api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API для 3D Quotation",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "fastify": "^4.25.0",
    "@fastify/cors": "^8.5.0",
    "dotenv": "^16.3.1"
  }
}
```

**Создай базовый `apps/api/server.js`:**

```javascript
import Fastify from 'fastify';

const app = Fastify({
  logger: true
});

app.get('/health', async (request, reply) => {
  return { status: 'ok', timestamp: new Date().toISOString() };
});

const start = async () => {
  try {
    await app.listen({ port: 3001, host: '0.0.0.0' });
    console.log('🚀 Server running on http://localhost:3001');
  } catch (err) {
    app.log.error(err);
    process.exit(1);
  }
};

start();
```

---

### Шаг 7: Установить зависимости

**Вернись в корень проекта:**

```bash
cd ../..

# Установить зависимости для всех приложений
pnpm install
```

---

### Шаг 8: Проверить что всё работает

**Терминал 1 — запусти фронтенд (выбери один вариант):**

**Вариант A: Через Node.js (рекомендуется для Windows):**
```bash
cd apps/web
npx http-server -p 3000 -c-1
```

**Вариант B: Через Python (если установлен):**
```bash
cd apps/web
python -m http.server 3000
```

**Вариант C: Через PowerShell (встроенный в Windows):**
```powershell
cd apps/web
# PowerShell 7+ (если у тебя pwsh)
python -m http.server 3000

# Или через .NET (если установлен)
dotnet serve -p 3000
```

**Вариант D: Просто открыть файл (для быстрой проверки):**
```bash
# Просто открой в браузере:
# file:///C:/YandexDisk/01 Projects/Current/Cursor Projects/3D-Quotation-Ecosystem/apps/web/index.html
```
⚠️ **Внимание:** При открытии файла напрямую могут не работать некоторые функции (например, fetch к API из-за CORS). Лучше использовать сервер.

Открой: http://localhost:3000

**Терминал 2 — запусти бэкенд:**
```bash
cd apps/api
node server.js
```

Открой: http://localhost:3001/health

**Должен увидеть:**
```json
{
  "status": "ok",
  "timestamp": "2024-12-10T12:00:00.000Z"
}
```

---

### Шаг 9: Закоммитить изменения

```bash
git add .
git commit -m "feat: setup monorepo with pnpm workspaces"
git push
```

---

## ✅ Definition of Done

Задача выполнена, когда:
- [x] Структура папок создана
- [x] pnpm-workspace.yaml настроен
- [x] Файлы перенесены в apps/web
- [x] apps/api создан с базовым сервером
- [x] Зависимости установлены
- [x] Фронтенд работает на :3000
- [x] Бэкенд работает на :3001
- [x] Изменения закоммичены

---

## 🆘 Если что-то не работает

### Ошибка: "pnpm: command not found"
**Решение:** Установи pnpm глобально через npm:
```bash
npm install -g pnpm
```

### Ошибка: "python: command not found" или "python не является внутренней командой"
**Решение (Windows):**

**Вариант 1: Используй Node.js сервер (рекомендуется):**
```bash
cd apps/web
npx http-server -p 3000 -c-1
```
`npx` автоматически скачает `http-server`, если его нет.

**Вариант 2: Установи Python:**
1. Скачай с https://www.python.org/downloads/
2. При установке отметь "Add Python to PATH"
3. Перезапусти терминал
4. Попробуй снова: `python -m http.server 3000`

**Вариант 3: Используй Live Server в Cursor/VS Code:**
1. Установи расширение "Live Server"
2. Правый клик на `index.html` → "Open with Live Server"

### Ошибка: "Cannot find module 'fastify'"
**Решение:**
```bash
cd apps/api
pnpm install
```

### Порт 3001 занят
**Решение:** Измени порт в `server.js`:
```javascript
await app.listen({ port: 3002 });
```

---

## 📞 Нужна помощь?

Просто напиши:
```
Застрял на шаге X.
Вот что делал: [опиши]
Вот что получил: [ошибка]
```

И я помогу! 🚀

---

**После выполнения:** Напиши "US-2.1 выполнена!" и покажи результат.

